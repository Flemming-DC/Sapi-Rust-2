use crate::tools::err::Fallible;
use crate::tools::{arena, stdstringStringExt, HashMapA, Pia, SliceOfTExt, StringAExt, VecA};
use crate::StringA;
use super::*;


pub fn analyze_insert(_names: &[Name], _root_indices: PrefixedTab, _tok_data: &TokData, _model: &DataModel) -> Fallible<Semantic> {
    unimplemented!() // find keys and update datamodel
}


